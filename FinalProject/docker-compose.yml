# docker-compose.yml
services:
  fastapi:
    build: ./fastapi
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
  bff:
    build: ./bff
    ports:
      - "3000:3000"
    depends_on:
      - fastapi
